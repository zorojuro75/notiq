global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: notiq-api
    static_configs:
      - targets: ["api:8080"]   # Docker service name

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]